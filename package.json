{
  "name": "heva-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && npm run build",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "ws": "^8.21.0"
  }
}
